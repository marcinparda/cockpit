# docs/ontology/

Ontology/graph config artifacts. This is a pointer index only — content lives in each linked file.

- [`ontology-sketch.md`](ontology-sketch.md) — design notes/sketch of the ontology
- [`ontology.json`](ontology.json) — ontology definition
- [`ontology.cypher`](ontology.cypher) — Cypher schema/queries for the ontology
- [`seed.cypher`](seed.cypher) — seed data for the graph DB
- [`neo4j-setup.sh`](neo4j-setup.sh) — Neo4j setup script
- [`mcp-config.json`](mcp-config.json) — MCP config for ontology tooling
